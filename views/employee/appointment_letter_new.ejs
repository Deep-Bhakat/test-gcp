<div class="container my-4  mx-auto rounded border">
    <h2 class="text-center my-2">Appointment Letter Form</h2>
    <div class="container mx-20 my-2 ">


        <form class="needs-validation" novalidate method="post" action="/employee/appointmentletter">
            <div class="row">
                <input type="hidden" name="emp_id" value="<%= employee.id %>" />
                <div class="col-md-3 mb-4 ">
                    <label for="validationCustom04 ">Type Of Employee</label>
                    <select class="form-select " name="type" id="validationCustom04 " required>
          <option value="">--Select--</option>
          <option value="general">General(Bangla-English)</option>
          <option value="general-hindi">General(Hindi-English)</option>
                    <option value="supervisor">Supervisor</option>
                    
          <option value="joining">Letter of Joining</option>
          <option value="trainee">Trainee</option>
        </select>
                    <div class="invalid-feedback ">
                        Please select type of employee.
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-3 mb-3">
                    <label for="validationCustom03">Designation</label>
                    <input type="text" class="form-control" id="validationCustom03" name="designation" placeholder="Enter Designation" required>

                    <div class="invalid-feedback">
                        Please enter Designation.
                    </div>
                </div>

                <div class="col-md-3 mb-3">
                    <label for="validationCustom03">Basic Salary</label>
                    <input type="number" class="form-control" id="basic_salary" name="basic_salary" onkeyup="calcGross()" placeholder="Enter Basic Salary" required>

                    <div class="invalid-feedback">
                        Please enter basic salary.
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <label for="validationCustom03">HRA</label>
                    <input type="number" class="form-control" id="hra"  name="hra" onkeyup="calcGross()" placeholder="Enter HRA">

                    <div class="invalid-feedback">
                        Please enter HRA.
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <label for="validationCustom03">Gross Salary</label>
                    <input type="number" class="form-control" id="gross_salary" name="gross_salary" readonly placeholder="Enter Gross Salary" required>

                    <div class="invalid-feedback">
                        Please enter Gross salary.
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-2 mb-3">
                    <button type="submit" class="btn btn-primary">View Appointment Letter</button></a>
                </div>
                <!-- <div class="col-md-2 mb-3">
                    <a href="/employee/appointment-super"><button type="button" class="btn btn-primary">View Super-Visor Appointment Letter</button></a>
                </div> -->

            </div>
            <!-- <button class="btn btn-secondary" type="button">Go Back</button> -->
            <!-- <a href="/employee/form2"> <button class="btn btn-success" type="button">Proceed</button></a> -->
        </form>

        <script>
            // Example starter JavaScript for disabling form submissions if there are invalid fields
            (function() {
                'use strict';
                window.addEventListener('load', function() {
                    // Fetch all the forms we want to apply custom Bootstrap validation styles to
                    var forms = document.getElementsByClassName('needs-validation');
                    // Loop over them and prevent submission
                    var validation = Array.prototype.filter.call(forms, function(form) {
                        form.addEventListener('submit', function(event) {
                            if (form.checkValidity() === false) {
                                event.preventDefault();
                                event.stopPropagation();
                            }
                            form.classList.add('was-validated');
                        }, false);
                    });
                }, false);
            })();
        </script>
    </div>
</div>

<script>
    function showPreview(event) {
        //document.getElementById("isImage").value = "1";
        if (event.target.files.length > 0) {
            let src = URL.createObjectURL(event.target.files[0]);
            //$("#photo").val(event.target.files[0].size/1024);
            let preview = document.getElementById("preview");
            preview.src = src;
            preview.style.width = "120px";
            preview.style.height = "150px";
            preview.style.display = "block";
        }
    }
    function calcGross(){
        var basic = parseInt($('#basic_salary').val());
        var hra = parseInt($('#hra').val());
        var gross=0;
      
        if(hra){
            gross = basic + hra;
        }else{
            gross = basic;
        }
      
        $('#gross_salary').val(gross);
    }
</script>
<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
<script src="http://cdnjs.cloudflare.com/ajax/libs/jquery.form/3.51/jquery.form.min.js"></script>